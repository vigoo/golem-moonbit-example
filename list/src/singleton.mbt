///| The state instance representing the editing of a single list. 
/// 
/// We can have one global state because we are going to instantiate the `list` component for each list separately.
pub let state : State = State::new()
