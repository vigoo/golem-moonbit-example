// Generated by `wit-bindgen` 0.36.0.

///|
pub fn deadline() -> UInt64? {
  if @src.state.is_archived() {
    None
  } else {
    let deadline = @src.state.email_deadline
    Some(deadline.unix_millseconds().reinterpret_as_uint64())
  }
}

///|
pub fn recipients() -> Array[String] {
  @src.state.email_recipients.iter().map(fn(email) { email._ }).to_array()
}
